<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/posts">a way？</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <div class="container">
      <div class="row">
        <% if current_user != nil %>
          <p><b><%= link_to "#{current_user&.nick_name}", admin_user_path(current_user), class: 'ml-3 text-dark' %> さんがログイン中</b></p>
        <% end %>
      </div>
    </div>
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          メニュー
        </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if current_user != nil %>
              <%= link_to '投稿する', new_post_path, class: 'dropdown-item' %>
              <%= link_to 'マイページ', admin_user_path(current_user), class: 'dropdown-item' %>
              <%= link_to 'ログアウト', logout_path, method: :delete, class: 'dropdown-item' %>
            <% else %>
              <%= link_to '新規登録', new_admin_user_path, class: 'dropdown-item' %>
              <%= link_to 'ログイン', login_path, class: 'dropdown-item' %>
            <% end %>
          </div>
      </li>
    </ul>
  </div>
</nav>